@model IEnumerable<TrazaNet.DataLayer.DENIERRED>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMDENIER)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCDENIER)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMDENIER)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCDENIER)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDDENIERRED }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDDENIERRED }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDDENIERRED })
        </td>
    </tr>
}

</table>
